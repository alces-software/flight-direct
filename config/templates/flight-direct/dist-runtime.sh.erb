#!/bin/bash
#
# This template/script contains the distribution information that
# flight-direct was built on. As such it is rendered at build time
# separately from the reset of the runtime environment
#
export cw_DIST=<%= cw_DIST %>
